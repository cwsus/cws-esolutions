## Target hostnames for the redirect
login.uri = /ui/login/default

expired.uri = /ui/user-account/password

## uri's that should be ignored
## if all uri's should be ignored, specify the value ALL
ignore.uri.list = /ui/login, /ui/online-reset, /ws